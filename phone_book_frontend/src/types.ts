export type Contact = {
  id: number;
  name: string;
  phoneNo: number;
  email: string;
  tags: string;
};

export type ContactDetails = {
  name: string;
  phoneNo: number;
  email: string;
  tags: string;
};
